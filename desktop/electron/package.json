{
  "name": "bootsmind-wikit",
  "version": "0.7.0",
  "private": true,
  "author": "ikey4u <pwnkeeper@gmail.com>",
  "description": "Bootsmind Wikit desktop shell using Rust native module through NAPI",
  "main": "src/main.js",
  "scripts": {
    "build:native": "cd ../native && npm install && npm run build",
    "start": "npm run build:native && electron .",
    "dev": "electron .",
    "serve": "npm run dev",
    "pack": "npm run build:native && electron-builder --dir",
    "dist": "npm run build:native && electron-builder",
    "dist:mac": "CSC_IDENTITY_AUTO_DISCOVERY=false npm run build:native && CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac dmg --publish never",
    "build:dmg": "node scripts/build-dmg.js"
  },
  "repository": "https://github.com/ikey4u/wikit.git",
  "devDependencies": {
    "electron": "35.0.0",
    "electron-builder": "^26.0.12",
    "electron-reloader": "^1.2.3"
  },
  "build": {
    "directories": {
      "output": "../../dist"
    },
    "appId": "com.bootsmind.wikit",
    "productName": "Bootsmind Wikit",
    "files": [
      "src/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../native",
        "to": "native",
        "filter": [
          "*.node",
          "index.js",
          "package.json"
        ]
      }
    ],
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "mac": {
      "icon": "icons/icon.icns",
      "target": [
        "dmg"
      ],
      "category": "public.app-category.reference",
      "gatekeeperAssess": false
    },
    "dmg": {
      "icon": "icons/icon.icns",
      "backgroundColor": "#f6f7f9",
      "window": {
        "width": 540,
        "height": 380
      },
      "contents": [
        {
          "x": 150,
          "y": 180,
          "type": "file"
        },
        {
          "x": 390,
          "y": 180,
          "type": "link",
          "path": "/Applications"
        }
      ]
    },
    "win": {
      "icon": "icons/icon.ico"
    },
    "linux": {
      "icon": "icons/128x128.png",
      "category": "Utility"
    }
  }
}
